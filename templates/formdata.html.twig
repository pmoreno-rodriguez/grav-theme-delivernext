{% extends 'partials/base.html.twig' %}

{% if form is null %}
    {% set form = grav.session.getFlashObject('form') %}
{% endif  %}

{% block content %}
  <div class="modular-row bottom ">
    {{ page.content|raw }}

    {% if form %}
    {% include 'partials/form-messages.html.twig' %}
    <div>
        {{ 'PLUGIN_FORM.DATA_SUMMARY'|t }}
         {% include "forms/data.html.twig" ignore missing %}
    </div>
    {% else %}
        <div class="alert alert-warning" role="alert"><p>{{ 'PLUGIN_FORM.NO_FORM_DATA'|t }}</p></div>
    {% endif %}
  </div>
{% endblock %}
